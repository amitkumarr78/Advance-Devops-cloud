pipeline {
    agent any

    stages {

        stage('Build Docker Image') {
            steps {
                dir('website') {
                    sh 'docker build -t website:v1 .'
                }
            }
        }

        stage('Run Test') {
            steps {
                sh 'docker images'
            }
        }
    }
}